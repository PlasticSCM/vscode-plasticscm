describe("Config", () => {
  context("has all keys defined", () => {
    // TODO: implement a test that checks:
    // a) that every 'contributes.configuration' key is defined in Config interface.
    // b) that every property in Config interface is defined as a 'contributes.configuration' key
  });
});
